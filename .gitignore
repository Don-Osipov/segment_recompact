# Rust build artifacts (the helper is built on install, not committed)
target/
plugins/segment_recompact/target/
plugins/segment_recompact/Cargo.lock

# bin/recompact is a tracked launcher script: it runs target/release/recompact when built
# locally, else downloads this version's release binary on first use.
